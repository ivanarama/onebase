name: КатегорииКонтента
title: Категории контента
hierarchical: true
# Рубрики новостей и статей. Иерархия — чтобы «Новости компании» жили внутри
# «Пресс-центра» без второго справочника.
search_fields: [Наименование, Слаг]
fields:
  - name: Наименование
    type: string
  - name: Слаг
    type: string
  - name: Описание
    type: string
  - name: Порядок
    type: number
