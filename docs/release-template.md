# Шаблон тела GitHub Release

Скопируй этот markdown в поле описания при создании Release на github.com — карточка релиза на onabase.site автоматически возьмёт из него красивое саммари и обложку.

---

## Как сайт превращает Release в карточку

Скрипт `releases.js` извлекает превью **в таком порядке** (первый сработавший — побеждает):

1. **HTML-маркер** `<!-- summary: текст -->` — самый надёжный, контроль 100%
2. **Секция с заголовком** из списка: `Что нового`, `Главное`, `Highlights`, `TL;DR`, `Summary`, `What's New`, `Overview`, `Кратко`, `Обзор`
3. **Fallback** — первый параграф любой секции, кроме `Установка / Installation / Requirements / Troubleshooting / Assets`, с пропуском строк-инструкций («Скачайте…», «1. Распакуйте…», `$ команда`)

**Обложка** работает по той же логике: первая картинка из любой *не-установочной* секции.

---

## Шаблон №1 — минимальный (для патч-релизов)

```markdown
<!-- summary: Исправили проблему с проведением документов и парой багов в SQLite-режиме. -->

## Что нового

Краткое исправление найденных в `v0.3.27` багов проведения документов и редкие падения SQLite на больших справочниках.

## Изменения

- 🐛 Исправлено: проведение документа `РасходныйОрдер` при пустой табличной части
- 🐛 Исправлено: падение SQLite при импорте >100k записей
- ⚡ Ускорена выборка остатков на 15%

## Установка

См. предыдущий релиз — формат не изменился.
```

---

## Шаблон №2 — полноценный (для minor/major релизов)

```markdown
<!-- summary: DSL-редактор в браузере, hot reload без перезапуска, обновлённый конструктор отчётов. -->

## 🎯 Главное

![Главная фича релиза](https://github.com/user-attachments/assets/your-cover.png)

Релиз посвящён девелоперскому опыту: теперь весь цикл разработки конфигурации можно делать прямо в браузере, без рестартов и переключений между окнами.

## ✨ Новое

- **DSL-редактор в браузере** — Monaco, подсветка синтаксиса, автодополнение по метаданным, переход к определению
- **Hot reload** — изменения YAML и DSL применяются за <100ms без рестарта сервера
- **Конструктор отчётов** — визуальное построение запроса с группировками и отборами

### DSL-редактор

![Скриншот редактора](https://github.com/user-attachments/assets/editor.png)

Полноценная среда разработки прямо в браузере…

### Hot reload

![Hot reload demo](https://github.com/user-attachments/assets/hot-reload.gif)

Изменили YAML — увидели результат…

## 🔧 Изменения

- `feat(dsl)`: API текущего пользователя — `ТекущийПользователь()`, `Роли()`
- `feat(api)`: REST endpoint для произвольных запросов
- `perf(sqlite)`: выборки на больших справочниках в 2× быстрее
- `fix(forms)`: фокус после сохранения вместо закрытия

## ⚠️ Breaking changes

> [!WARNING]
> Удалён устаревший хук `ПередЗаписьюУстаревший()`. Используйте `ПередЗаписью()`.

## 📦 Установка

### Windows
1. Скачайте `onebase-windows-amd64.zip` (на Windows ARM — `onebase-windows-arm64.zip`,
   в нём нет `onebase-gui.exe`: лаунчер откроется в браузере)
2. Распакуйте в любую папку
3. Запустите `onebase-gui.exe`

### Linux
```bash
tar xzf onebase-linux-amd64.tar.gz     # Raspberry Pi, NAS: onebase-linux-arm64.tar.gz
./onebase-linux-amd64/onebase start
```

Обновляете установленную копию — замените файл в PATH, иначе команда `onebase`
продолжит звать старую версию (архив распаковывается в подкаталог и сам ничего
не заменяет):

```bash
sudo mv onebase-linux-amd64/onebase /usr/local/bin/onebase
onebase version                        # версия, коммит и путь исполняемого файла
```

### macOS
```bash
tar xzf onebase-darwin-arm64.tar.gz    # Apple Silicon; Intel: onebase-darwin-amd64.tar.gz
./onebase start
```

## 🙏 Благодарности

Спасибо @user1, @user2 за PR и баг-репорты.
```

---

## Что попадёт на сайт

С шаблоном №2 карточка релиза покажет:

- **Обложка**: `your-cover.png` (первая картинка в `## 🎯 Главное`, картинки из `## Установка` игнорируются)
- **Заголовок**: тег релиза или Release Name, если задан
- **Саммари**: «DSL-редактор в браузере, hot reload без перезапуска, обновлённый конструктор отчётов.» (из `<!-- summary -->`-маркера)
- **Бейдж типа**: `minor` (вычисляется автоматически из semver, сравнивая с предыдущим стабильным релизом)
- **Дата публикации** — берётся из GitHub автоматически

На странице релиза (`release.html?tag=...`) — полный markdown с картинками, видео, alerts, code blocks и таблицами.

---

## Workflow

1. `git tag v0.X.Y && git push --tags`
2. На github.com → **Draft new release** → выбрать тег
3. **Release name**: оставить пустым (тогда покажется `v0.X.Y`) или ввести осмысленное название
4. **Describe this release**: вставить шаблон, заполнить `<!-- summary -->`, перетащить скриншоты прямо в текстовое поле (загрузятся в GitHub user-attachments)
5. **Publish release** — через ~15 минут (или сразу при hard refresh страницы) появится на сайте

---

## Советы

- **Скриншоты**: до 1–2 MB каждый, ужимать через [squoosh.app](https://squoosh.app/) до коммита
- **Видео**: лучше MP4 через `<video>` чем GIF — в 10× меньше; перетащить как файл, GitHub конвертирует автоматически
- **Эмодзи в заголовках** работают — `## 🎯 Главное` парсится корректно, эмодзи зачищается перед сравнением
- **GitHub Alerts** (`> [!NOTE]`, `> [!WARNING]`) рендерятся как красивые блоки в стиле GitHub
- **Тестовый превью**: после публикации открой `https://onabase.site/release.html?tag=v0.X.Y` и посмотри как выглядит. Если что-то не так — отредактируй release на GitHub, через 15 минут изменения подхватятся

## Кеш

Сайт кеширует список релизов в localStorage браузера на 15 минут. Чтобы увидеть свежий релиз сразу — `Ctrl+Shift+R` (hard reload) или в DevTools → Application → Local Storage → удалить ключи `onebase-releases-cache-v2:*`.
