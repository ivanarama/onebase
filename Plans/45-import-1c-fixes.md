# План 45 — Исправления импорта 1С и UX конфигуратора (issue #26, #28, #27)

## Контекст

Три открытых issue от тестировщиков:

- **#28** — конфигуратор запускается с полностью развёрнутым деревом метаданных; нужно запускать со свёрнутым (как в 1С).
- **#26** — импорт «традиционной» конфигурации 1С (справочник + 2 формы + шаблон + логотип + подсистема) даёт 5 дефектов конвертера `internal/converter`.
- **#27** — «подбор по остаткам» в табличную часть документа (РеализацияТоваров). Обсуждается отдельно, реализуется после #26/#28.

## Объём этой итерации

Делаем **#28** и **#26** (полностью, включая формы и шаблоны). **#27** — спроектирован ниже, реализация после обсуждения.

---

## #28 — запуск со свёрнутым деревом

**Файл:** `internal/launcher/configurator_tmpl.go`

Группы дерева — `<details open class="cfg-tree" data-group="...">` (~11 шт., строки ~3465–3543).
- Убрать атрибут `open` у всех групповых `<details class="cfg-tree">`.
- Стрелка-индикатор синхронизируется init-скриптом (строки 3424–3428, `u()`), поправит на `▸`. Хардкод `▾` в `<span class="tree-toggle">` поменять на `▸` чтобы не мигало.
- Поиск (`filterTree`) раскрывает совпадения — не трогаем.

---

## #26 — дефекты импорта (пакет `internal/converter`)

### П.5 — фантомная константа (тривиально)
`writer/report.go:36`: правое число захардкожено `→ 1 YAML`. Заменить на `r.Constants` (как в остальных строках).

### П.1 — лишние справочники (подсистема/логотип)
`parser1c/metadata.go` `ParseDir` (строки ~270–300). Инвертировать: **только известные прикладные каталоги → объекты; всё прочее → `SkippedDirs` с пометкой.** Удалить «convert as catalogs» список и `default → parseCatalogs`; служебные разделы (Subsystems, CommonForms, CommonTemplates, Roles, Languages, …) — в skip.

### П.2 — Код/Наименование и ТЧ у справочника
- `WriteCatalogs` (`writer/yaml.go:44`): добавлять стандартные `Код`/`Наименование` (string) в начало `Fields`, если их нет (case-insensitive).
- `CatalogMeta` (`types.go`): добавить `TabularSections []TabularSection`; распарсить в `parseCatalogs` (v8.3 `ChildObjects.TabularSections` и старый `props.TabularSections`); `yamlCatalog` + `tableparts,omitempty` (загрузчик уже поддерживает — `metadata/yaml.go:45`).

### П.4 — импорт форм
Интегрировать готовый `internal/onec_forms.ImportFromOneC` в bulk-конвертер. При обходе `Catalogs/<X>/Forms/<F>/Ext/Form.xml` (+ `Form/Module.bsl`, `Form/Items/`) и аналогично у документов — звать `ImportFromOneC` с `EntityName=<X>`, писать в `<out>/forms/<x>/`. Счётчик/предупреждения в `ConversionReport`.

### П.3 — импорт шаблонов (best-effort)
1С-шаблоны: `<Object>/Templates/<N>/Ext/Template.*` и `CommonTemplates/<N>/`. Полная конвертация `.mxl` нецелесообразна. Создавать scaffold printform в `printforms/<owner>/<name>.yaml` (шапка+таблица по реквизитам владельца, пометка «оформление перенести вручную»), исходник копировать как `<name>.mxl.src`. Счётчик в отчёте.

**Тесты:** `parser1c/parsedir_test.go`, `convert_test.go`, `writer/yaml_test.go` — кейсы на п.1/п.2/п.5/форму/шаблон.

---

## #27 — подбор по остаткам (проект, реализация после обсуждения)

Кнопка «Подбор по остаткам» у табличной части документа: диалог со списком номенклатуры и остатком по складу документа; отметка позиций+количества → строки в ТЧ.

Переиспользовать `storage.GetBalances` (`register.go:301`), реф-пикер UI (`templates.go:1126`, `addTpRow:1144`).
- Эндпоинт `GET /ui/document/<entity>/_pickbalance?register=&warehouse=&tp=` → JSON остатков.
- Конфиг ТЧ: `pick_balance:{register,item_field,qty_field,warehouse_field}` (расширить `rawTablePart`/`TablePart`).
- Кнопка + JS-диалог в `templates.go`; локализация в `i18n/locales`.

**Открытые вопросы к обсуждению:** откуда брать имя регистра остатков (явный конфиг vs автоопределение); фильтр по складу; учёт резервов; поведение для документов без склада.

---

## Verification
- `go test ./...` зелёный.
- Ручной импорт тестовой выгрузки (справочник+форма+шаблон+логотип+подсистема), сверка `conversion_report.txt` с ожиданиями #26.
- Конфигуратор: дерево свёрнуто на старте, поиск/разворачивание работают.
